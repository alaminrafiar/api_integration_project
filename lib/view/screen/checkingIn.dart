import 'package:flutter/material.dart';

class chekin extends StatelessWidget {
  const chekin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
